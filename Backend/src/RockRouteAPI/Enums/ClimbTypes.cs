namespace RockRoute.enums
{
    public enum climbTypes
        {
            Trad, //0
            Sport, //1 //For if the user tries to make a new account with existing email
            Boulder, //2
        }
}